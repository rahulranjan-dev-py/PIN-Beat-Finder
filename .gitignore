# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android Studio / IntelliJ
.idea/
*.iml
local.properties
captures/
.externalNativeBuild/
.cxx/

# Kotlin
.kotlin/

# OS
.DS_Store

# Release signing secrets — never commit
keystore.properties
*.jks
*.keystore

# The debug keystore is intentionally committed so every CI build shares one signature
# (otherwise each release refuses to install over the previous one).
!app/debug.keystore
