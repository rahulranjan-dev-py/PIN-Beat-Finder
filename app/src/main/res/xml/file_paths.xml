<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Matches ExcelSyncManager.EXPORT_DIR (cacheDir/exports) -->
    <cache-path name="exports" path="exports/" />
    <!-- Matches CrashReporter.DIR (filesDir/crashes) -->
    <files-path name="crashes" path="crashes/" />
</paths>
